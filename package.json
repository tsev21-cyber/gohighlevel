{
  "name": "lead-engine-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Demo: Meta Ads -> Landing -> AI Bot -> Lead Qualification -> GoHighLevel CRM",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1",
    "express": "^4.21.2"
  }
}
